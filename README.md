# classcart

This project has been converted from a static HTML landing page into a Python Streamlit app.

## Run locally

1. Install dependencies:
   pip install -r requirements.txt
2. Start the app:
   streamlit run app.py

The app includes a dark-themed hero section, searchable product catalog, feature highlights, and a cart sidebar.